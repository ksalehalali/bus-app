import 'package:flutter/material.dart';

class OutgoingWallet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('Outgoing Wallet', style: TextStyle(fontSize: 32.0),)
      ),
    );
  }
}