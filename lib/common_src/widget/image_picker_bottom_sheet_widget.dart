import 'dart:html';
import 'package:flutter/material.dart';

class ImagePickerBottomSheetWidget extends StatelessWidget {
  final String text;

  const ImagePickerBottomSheetWidget({Key? key, required this.text}) : super(key: key);

  @override
  Widget build(BuildContext context) {
   // late Reference ref;
    File? file = null;
    return Text('hdshdsh');
  }
}